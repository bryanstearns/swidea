# Set the application name here - everything below is boilerplate.
set :application, "swidea"
# --- 
load 'deploy'
require 'myrecipes'
Dir['vendor/plugins/*/recipes/*.rb'].each { |plugin| load(plugin) }
